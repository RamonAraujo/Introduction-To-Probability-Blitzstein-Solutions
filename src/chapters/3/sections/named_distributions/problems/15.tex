$$F(x) = P(X \le x) = \begin{cases}  0 &, \text{ for } x < 1\\ \frac{\lfloor x \rfloor}{n} &, \text{ for } 1 \le x \le n\\ 1 &, \text{ for } x > n \end{cases}$$  , \\
where $\lfloor x \rfloor$ is the floor function, equal to the largest integer that is less than or equal to $x$ .